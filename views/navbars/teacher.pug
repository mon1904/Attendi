.navbar.navbar-default(role='navigation')
  .container
    .navbar-header
    button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
      span.sr-only Toggle navigation
      span.icon-bar
      span.icon-bar
      span.icon-bar
    a.navbar-brand(href='/') Attendi
      #navbar.collapse.navbar-collapse
        ul.nav.navbar-nav
          button.cl
            a(href='/') Home
          button.dropdown
            a(href='/school-classes/teacher') Class
          button.dropdown
            a(href='/attendance/teacher') Attendance
          button.dropdown
              a(href='/messages/parent') Contact Parent
              if notifCnt !== undefined
                span (#{notifCnt})
          button.dropdown
              a(href='/stats/teacher/'+user._id) Statistics
          button.dropdown
              a(href='#',data-toggle='dropdown').dropdown-toggle Profile
              b.caret
              ul.dropdown-menu
                li: a(href='/users/edit/'+user._id) Edit Profile
                li: a(href='/users/view/'+user._id) View Profile
        ul.nav.navbar-nav.navbar-right
          button
            a(href='/users/logout') Logout
